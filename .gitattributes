# Normalize all text files to LF on checkout (works correctly on Linux/Render)
* text=auto eol=lf

# Binary files — no conversion
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.db  binary
*.pyc binary
